<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RM-SH Home</title>
    <link rel="icon" href="https://cdn.discordapp.com/attachments/1005773256151810078/1346366087343767583/1000x983.png?ex=67c7eca1&is=67c69b21&hm=9989ad43e159e25c8a2dae9c8067c054630819b826a6f64e17663c33623fea95&" type="image/icon type">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-r from-red-600 via-blue-400 to-white">

    <!-- Navbar -->
    <nav class="bg-gradient-to-r from-red-600 via-blue-400 to-white p-4 flex shadow-md justify-between items-center">        
        <div class="flex-1 text-center">
            <div class="inline-block text-3xl drop-shadow-md font-bold text-red-800 hover:text-red-600 hover:scale-105 transition duration-300 cursor-pointer">
                <a href="/">RM Shopping Hub</a>
            </div>
        </div>
    </nav>

    <div class="p-4 mt-4 flex justify-between items-center">
        <div class="text-4xl text-blue">
            <p>Hello <%= buyer.name %> !</p>
        </div>

        <div class="flex space-x-4">
            <a href="/buyers/cart" class="px-4 py-1 bg-pink-500 text-white rounded-full hover:bg-pink-400 hover:scale-125  transition duration-200 inline-block text-sm text-center">Cart</a>
            <a href="/buyers/profile" class="px-4 py-1 bg-blue-500 text-white rounded-full hover:bg-blue-400 hover:scale-125  transition duration-200 inline-block text-sm text-center">Profile</a>
            <a href="/buyers/wishlist" class="px-4 py-1 bg-yellow-500 text-white rounded-full hover:bg-yellow-400 hover:scale-125  transition duration-200 inline-block text-sm text-center">Wishlist</a>
            <a href="/buyers/logout" class="px-4 py-1 bg-red-500 text-white rounded-full hover:bg-red-400 hover:scale-125 transition duration-200 inline-block text-sm text-center">Logout</a>
        </div>
    </div>

    <!-- Error/Success -->
    <div class="max-w-lg mx-auto mt-6">
        <% if (success && success.length>0) { %>
            <div class="bg-green-100 text-green-800 text-center p-4 rounded-lg mb-4">
                <p><%= success %></p>
            </div>
        <% } %>

        <% if (error && error.length>0) { %>
            <div class="bg-red-100 text-red-800 text-center p-4 rounded-lg mb-4">
                <p><%= error %></p>
            </div>
        <% } %>
    </div>

    <div class="max-w-7xl mx-auto mt-8 p-6 bg-transparent">
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
            <% if (products && products.length > 0) { %>
                <% products.forEach(function(prod) { %>
            <a href="/buyers/products/<%= prod._id %>" class="bg-transparent p-4  hover:scale-125 transition duration-300">
                <div class="w-full h-64">
                    <img src="data:image/jpeg;base64,<%= prod.image.toString('base64') %>" alt="Product 2" class="w-full h-full object-cover rounded-lg shadow-md">
                </div>
            </a>
            <% }); %>
        <% } else { %>
            <p class="text-gray-700 text-lg col-span-4 text-center">No products available.</p>
        <% } %>
        </div>
    </div>

</body>
</html>
